@replit  
أريدك أن تبني أو تحدّث مشروعي الحالي ليصبح **ARC Intelligence Framework v2.0**  
وهو نظام تشغيل ذكاء مؤسسي متكامل يعمل على Replit + Supabase + n8n + OpenAI + ElevenLabs.  

🧩 ملاحظة مهمة:
تحقّق أولاً من الملفات الموجودة في المشروع:  
- إذا كانت الملفات أو المجلدات موجودة مسبقًا، **لا تعيد إنشائها ولا تكرّرها.**  
- فقط أضف أو حدّث الأجزاء الناقصة.  
- استخدم أسلوب “smart merge” لتوحيد الملفات بدلاً من استبدالها بالكامل.  

=========================================
⚙️ الجزء 1 — Bootstrap Script
=========================================

أنشئ أو عدّل ملف الجذر:
**arc_bootstrap.js**

يُنشئ هذا الملف بنية النظام (arc_core / actions / reports)  
ويُهيئ "Mr.F Brain" مع روابط Supabase وn8n وOpenAI وElevenLabs.

قبل الإنشاء:
> ✅ إذا وجدت مجلد arc_core أو brain_manifest.json لا تعيد إنشاؤه.

ضع هذا الكود بداخله (أضف فقط الأجزاء الناقصة):

-----------------------------------------
import fs from "fs"; import path from "path"; import fetch from "node-fetch"; import { createClient } from "@supabase/supabase-js";
console.log("🚀 Initializing ARC Intelligence Framework v2.0...");

const env={SUPABASE_URL:process.env.SUPABASE_URL,SUPABASE_KEY:process.env.SUPABASE_SERVICE_ROLE_KEY,OPENAI_KEY:process.env.OPENAI_API_KEY,N8N_URL:process.env.N8N_WEBHOOK_URL,ELEVEN_KEY:process.env.ELEVENLABS_API_KEY};
for(const[k,v]of Object.entries(env))if(!v)console.warn("⚠️ Missing "+k);

// Create folders if missing
["arc_core","arc_core/actions","reports","archives"].forEach(d=>{
 if(!fs.existsSync(d)){fs.mkdirSync(d,{recursive:true});console.log("📁 Created "+d)}
});

// Initialize brain manifest if not exists
const mpath="arc_core/brain_manifest.json";
if(!fs.existsSync(mpath)){
 const manifest={
  system_version:"ARC-v2.0",
  initialized_at:new Date().toISOString(),
  environment:{supabase_url:env.SUPABASE_URL,n8n_webhook:env.N8N_URL,replit_runtime:true},
  agents:[
    {id:"Mr.F",role:"Executive Brain"},
    {id:"L0-Comms",role:"Communications Director"},
    {id:"L0-Ops",role:"Operations Commander"},
    {id:"L0-Intel",role:"Intelligence Analyst"},
    {id:"Dr.Maya",role:"Research Analyst"},
    {id:"Jordan",role:"Creative Director"}
  ],
  modules:["Actions","Context","Reasoning","Execution"],
  status:"operational"
 };
 fs.writeFileSync(mpath,JSON.stringify(manifest,null,2));
 console.log("🧠 Brain manifest created");
}else{
 console.log("🧠 Existing brain manifest found — skipping creation");
}

// Create base actions only if missing
const actions={
 "system.report_generate.js":`export default{async execute(){const f="./reports/Report_"+Date.now()+".txt";fs.writeFileSync(f,"ARC Report "+new Date().toLocaleString());return{status:"generated",path:f}}}`,
 "comm.notify_telegram.js":`export default{async execute({message}){await fetch(process.env.N8N_WEBHOOK_URL,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text:message})});return{status:"sent"}}}`
};
for(const[n,c]of Object.entries(actions)){
 const fp="arc_core/actions/"+n;
 if(!fs.existsSync(fp)){fs.writeFileSync(fp,c);console.log("🧩 Added action: "+n)}
}

fs.writeFileSync("reports/Init_"+Date.now()+".txt","ARC Init "+new Date().toLocaleString());
console.log("✅ ARC Intelligence Framework v2.0 Setup Complete");
-----------------------------------------

=========================================
🎨 الجزء 2 — Dashboard (React)
=========================================

تحقّق:
> إذا كان لديك مجلد `client/`، لا تنشئ مشروع جديد  
> فقط أضف صفحة جديدة `Dashboard.tsx` أو حدّثها إذا ناقصة.

ملف: `client/src/pages/Dashboard.tsx`

-----------------------------------------
import { useEffect,useState } from "react";

export default function Dashboard(){
const[b,setB]=useState<any>(null);const[l,setL]=useState(true);const[t,st]=useState("");const[p,sp]=useState(false);
async function load(){try{const r=await fetch("/arc_core/brain_manifest.json");setB(await r.json());setL(false);}catch(e){console.error(e)}}
async function speak(){sp(true);const r=await fetch("/api/tts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text:t,voice:"alloy"})});const d=await r.json();new Audio("data:audio/mpeg;base64,"+d.audio).play();setTimeout(()=>sp(false),4e3)}
useEffect(()=>{load();const i=setInterval(load,7e3);return()=>clearInterval(i)},[]);
if(l)return<div className="p-8 text-gray-400">Loading Mr.F Brain...</div>;
return<div className="min-h-screen bg-black text-gray-100 p-8 space-y-8"><h1 className="text-3xl font-bold text-green-400">ARC Dashboard</h1>
<section className="bg-gray-900 p-6 rounded-xl border border-gray-800"><h2 className="text-2xl text-green-300 mb-4">🧠 Brain</h2><pre className="text-sm bg-black p-4 rounded-xl overflow-x-auto">{JSON.stringify(b,null,2)}</pre></section>
<section className="bg-gray-900 p-6 rounded-xl border border-gray-800"><h2 className="text-2xl text-green-300 mb-4">🎙️ Voice</h2>
<textarea className="w-full bg-gray-800 p-2 rounded-lg text-white mb-3" rows={3} placeholder="اكتب ما تريد أن يقوله Mr.F..." value={t} onChange={e=>st(e.target.value)}/>
<button disabled={p||!t} onClick={speak} className={p?"bg-gray-600 px-6 py-2 rounded-lg":"bg-green-500 hover:bg-green-600 text-black px-6 py-2 rounded-lg"}>{p?"🔊 يتحدث...":"تشغيل الصوت"}</button></section></div>}
-----------------------------------------

ثم تأكد من:
`client/src/main.tsx` يحتوي:
-----------------------------------------
import React from "react";
import ReactDOM from "react-dom/client";
import Dashboard from "./pages/Dashboard";
ReactDOM.createRoot(document.getElementById("root")!).render(<Dashboard />);
-----------------------------------------

=========================================
🎙️ الجزء 3 — ElevenLabs Voice API
=========================================

تحقّق إن لم يوجد الملف:
**server_tts.js**

-----------------------------------------
import express from "express";import fetch from "node-fetch";
const app=express();app.use(express.json());
app.post("/api/tts",async(req,res)=>{const{text}=req.body;const k=process.env.ELEVENLABS_API_KEY;
const r=await fetch("https://api.elevenlabs.io/v1/text-to-speech/pNInz6obpgDQGcFmaJgB",{method:"POST",headers:{"xi-api-key":k,"Content-Type":"application/json","Accept":"audio/mpeg"},body:JSON.stringify({text})});
const b=Buffer.from(await r.arrayBuffer());res.json({audio:b.toString("base64"),status:"ok"});});
app.listen(5050,()=>console.log("🎧 Voice API running"));
-----------------------------------------

=========================================
📡 الجزء 4 — Backend Auto-Link
=========================================
تحقّق إن لم يوجد الملف:
**arc_link_backend.js**

-----------------------------------------
import fetch from "node-fetch";
const n8n=process.env.N8N_WEBHOOK_URL;const s=process.env.SUPABASE_URL;
if(n8n)await fetch(n8n,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({system:"ARC v2.0",status:"linked"})});
console.log("✅ ARC backend link verified:",s);
-----------------------------------------

=========================================
🎯 النتيجة:
- لا تكرار لأي ملفات موجودة
- فقط إنشاء الملفات الناقصة
- النظام الكامل (Mr.F Brain) جاهز
- صوت ElevenLabs مفعّل
- Dashboard تفاعلي جاهز